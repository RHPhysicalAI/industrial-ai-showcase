apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: vla-warehouse
  namespace: factory-b
  annotations:
    serving.knative.dev/rollout-duration: "0"
spec:
  predictor:
    minReplicas: 1
    maxReplicas: 1
    tolerations:
    - key: nvidia.com/gpu
      operator: Exists
      effect: NoSchedule
    model:
      modelFormat:
        name: vllm
      storageUri: hf://openvla/openvla-7b
      resources:
        limits:
          nvidia.com/gpu: '1'
          cpu: '4'
          memory: 16Gi
        requests:
          nvidia.com/gpu: '1'
          cpu: '2'
          memory: 8Gi
