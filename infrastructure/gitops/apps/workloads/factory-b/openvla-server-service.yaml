apiVersion: v1
kind: Service
metadata:
  name: openvla-server
  namespace: factory-b
  labels:
    app: openvla-server
spec:
  selector:
    app: openvla-server
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
