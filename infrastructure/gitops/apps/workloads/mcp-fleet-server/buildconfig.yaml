apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: mcp-fleet-server
  namespace: agentic-ops
spec:
  output:
    to:
      kind: ImageStreamTag
      name: mcp-fleet-server:latest
  source:
    type: Binary
    binary: {}
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: infrastructure/gitops/apps/workloads/mcp-fleet-server/Dockerfile
  triggers: []
