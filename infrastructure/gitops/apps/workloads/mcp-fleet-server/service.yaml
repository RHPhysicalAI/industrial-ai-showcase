apiVersion: v1
kind: Service
metadata:
  name: mcp-fleet-server
  namespace: agentic-ops
  labels:
    app: mcp-fleet-server
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: mcp-fleet-server
  type: ClusterIP
