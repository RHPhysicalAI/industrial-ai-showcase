# This project was developed with assistance from AI tools.
apiVersion: v1
kind: Service
metadata:
  name: agentic-orchestrator
  annotations:
    description: LangGraph-based agentic orchestrator service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: agentic-orchestrator